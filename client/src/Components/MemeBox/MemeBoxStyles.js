import { makeStyles } from '@material-ui/styles';

const MemeBoxStyles = makeStyles(theme => ({
  modal: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    opacity: '0.85',
  },
}));

export default MemeBoxStyles;
