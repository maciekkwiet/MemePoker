import { makeStyles } from '@material-ui/core/styles';

const observerCheckboxStyles = makeStyles(() => ({
  tooltip: {
    '& .MuiTooltip-tooltip': {
      fontSize: 21,
    },
  },
}));

export default observerCheckboxStyles;
