import { makeStyles } from '@material-ui/styles';

const KickButtonStyles = makeStyles(theme => ({
  kickButton: {
    opacity: '0',
  },
  kickButtonHover: {
    opacity: '1',
  },
}));

export default KickButtonStyles;
